---
title: "Facilitator guide – Applied Replication for Data Skills"
subtitle: "Facilitator run sheet – full-day format"
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    theme: cosmo
execute:
  echo: false
---

::: {.callout-note}
**This document is for the facilitator's eyes only.** It is rendered as part of the site but not linked in the navbar. It carries the full run sheet, answer keys, and crib facts – including the Block 2 story of the data-preparation slip, which the deck tells openly.
:::

---

## Delivery lifecycle

The site is evergreen between deliveries. Scheduling one is a three-step edit; resetting after one is a four-step export.

**Scheduling a delivery.**

- [ ] Fill the `delivery:` block in `_variables.yml` (date, venue/platform, event, mode, registration URL) and set `active: 'true'`. Re-render and push; the landing page grows a "Next delivery" callout. Set `active: 'false'` again afterwards.
- [ ] Decide the extended-universe option at **T-2 weeks** against the go/no-go checklist in `_planning/extended-universe-spec.md`. If GO, the opt-in text there goes into the joining email.
- [ ] Decide recording. If recording: talk segments only, announced at the start of each recorded stretch, paused for hands-on and chat-heavy segments. Nothing in the day depends on the recording either way.
- [ ] Optionally create the prereg mirror form from `_planning/prereg-form-spec.md` and paste its URL into `_variables.yml`. The rendered declaration block is the primary record; the form is a silent secondary. Skipping it costs nothing.

**Resetting after a delivery (before the next one).**

- [ ] Export the responses Sheet (File → Download → CSV) and commit it as a date-stamped snapshot (e.g. `data/class_results_2026-06-16.csv`); refresh `data/class_results.csv` per `facilitator/live-results-setup.md` if you want the archived cohort visible on the evergreen site.
- [ ] Clear (or archive to a second tab) the old rows in the live Sheet so the next cohort's chart opens near-empty. **Do not touch the Form, the Worker, or the field schema** – the pipeline is working; only the rows rotate.

---

## The week before

- [ ] Click once through the Block 3 tab choreography (deck → the spec-menu page → the results page and back) so the switching order is in your fingers – it is the one sequence the day performs live several times.
- [ ] Send joining instructions: setup page link (two routes), the two-window/two-device tip, the optional pre-reading (listed on the setup page), and – if GO – the extended-universe opt-in text.
- [ ] Confirm the deck's Multi100 slide against the registered protocol at osf.io/7snkz (the note on that slide asks for the registration's actual wording, not a paraphrase).

## The night before

- [ ] Render the full site: `quarto render` from the workspace root; check no errors; push; confirm the Pages build completes on GitHub Actions.
- [ ] **Live results loop** (full setup + checks in `facilitator/live-results-setup.md`): submit one anonymous test row via the Form (incognito), confirm it reaches the Sheet, confirm the Worker returns the sheet as CSV, press *Run Code* on the live [Multiverse]({{< var live-results-url >}}) page and see the test dot. Then delete the test row.
- [ ] **Prereg block loop**: open the workspace zip fresh (download it as a participant would), complete the declaration block with a dummy spec, render, and check the rendered block and timestamp. If the mirror form exists, submit one dummy row and delete it.
- [ ] **Browser lab cold load**: open `exercise/browser-lab.qmd`'s live page in a private window on the venue-typical connection; time the webR engine load; run the first chunk and the Task 3 reproduce chunk (t = −3.804).
- [ ] Confirm both OSF nodes load logged-out: osf.io/6zqct (fork) and osf.io/8rtwe (archive); confirm osf.io/h7432 shows the replication component (the scavenger target).
- [ ] Prepare the catch-up card, the help doc, and the day-plan message (texts below) as ready-to-paste snippets.
- [ ] Charge everything; second device signed in to Teams as a participant-view check.

## Opening the (virtual) room – 30 minutes before

- [ ] Open the Teams meeting early; enable chat for everyone; check breakout rooms are available (they are optional – nothing depends on them).
- [ ] Seed the chat: site URL · browser-lab URL · cheatsheet URL · the timestamped day plan (the standing order if the host drops: "follow the day plan; finish the current task or take the break early; I will rejoin").
- [ ] Post the interaction key: "green circle emoji = working · STUCK = I need help · answers to quizzes: type but hold until I say go".
- [ ] Share the deck; keep three tabs ready to switch: the deck, the spec menu, the Multiverse page.
- [ ] If recording: announce it now and state what is and is not recorded.

---

## Run sheet

Times are block-relative. B1 starts at the advertised hour; 15-minute breaks after B1 and B3; 45-minute lunch after B2.

### Block 1 – One paper, three verdicts (90′)

| Rel. | Segment | What I do | What participants do | Watch out for |
|---|---|---|---|---|
| 0:00 | Welcome + what you do not need (8′) | Premise in two sentences ("*Nature* asked three times whether social science holds up – I was inside one of the papers; today you sit where I sat"). Roadmap. "Open the lab tab / your workspace NOW." | Open lab tab (engine warms) or Positron workspace; type one word in chat for why they came. | Late joiners: greet in chat, never restart. Two-device tip again. |
| 0:08 | Talk: the three Rs (20′) | Slides at measured pace; quiz beats as chat waterfalls (type, hold, "go"). | Chat waterfalls. | If behind after Miske, cut the moderators slide to one line. |
| 0:28 | Talk: the EU-frames case (14′) | Claim card verbatim; the five-analyst forest **one row at a time**; end on "you are analyst no. 6 – and the code is mine". | Watch the reveal. | Never rush the reveal. |
| 0:42 | Stretch (3′) | Cameras off, stand up. | Stretch. | Actually stop talking. |
| 0:45 | Hands-on 1: Tasks 1–3 (35′) | Drive Positron / the browser lab on the shared screen alongside them; monitor chat + help doc at 0:55, 1:05, 1:15; buddy-chunk any STUCK on my screen. Then the waterfall: "type your t, hold, go". | Fix the vocabulary (Task 1), meet the EU-frames panel (Task 2), fit the constrained model (Task 3); land **t = −3.804**; waterfall the t. | The wall of identical −3.804s is the payoff – wait for a critical mass before "go". Dead compute → cheatsheet ladder. |
| 1:20 | One number too many + break (10′) | Achievement line in chat; then open the thread for the afternoon with the deck slide "One number too many": you reproduced **t = −3.804**, but a *second* number, **t = −3.853**, sits in this analysis's public trail – I wrote both. Say the gap has a story and that I tell it after the break. Post break-return time in chat; countdown slide. | Name the achievement; break. | Hold the reveal: name the second number, but keep its cause for the told story that opens Block 2 – the payoff depends on the pause. |

### Block 2 – The record's second number, then the question underneath (90′)

| Rel. | Segment | What I do | What participants do | Watch out for |
|---|---|---|---|---|
| 0:00 | The story of the second number (20′) | Told, not hunted. Walk the deck's reveal sequence: "The other number in the record" (−3.804 vs −3.853; the two builds diverge on 2009 alone, every other year flat) → "One file, read twice" (`ZA4819` read a second time in place of `ZA4994`, so 2009 is 2008 respondents relabelled) → "The discovery and the correction" (my April-2025 annotation; the open OSF fork trail – `moreh_rep/` as-submitted, `moreh_rep_vAPR2025/` corrected script + exported data, `EUframes_cy.csv` now in 6zqct's main folder, 8rtwe uncorrected only) → "The same slip, one desk over" (KQXUE's double-appended wave) → "This is reproducibility working". | Watch; react. Anyone who wants to see the divergence for themselves opens the optional `forensics.R` exhibit. | Tell it slowly – it is the morning's hinge. Not a gotcha about the original author or me; land the moral (the open trail is what let the slip be caught) before changing gear. |
| 0:20 | Stretch (3′) | Cameras off, stand up. | Stretch. | Actually stop talking. |
| 0:23 | Talk: estimands (30′) | Red-card → A&B's four estimands → Lundberg → *Revenons*. Estimand waterfall on the *Revenons* slide: read 3–4 aloud, point at the spread. | Type their one-sentence estimand; hold; send on "go". | Silent room: name two people gently, or take reactions; two answers from twenty is a proceed. |
| 0:53 | Talk: DAG crash course (30′) | Confounder/mediator/collider; open/closed paths; which effect; spurious-by-design; A&B resolution; end in dagitty. | Watch; optionally open dagitty.net alongside. | Task 4 (the DAG) hands-on is AFTER lunch – resist starting it now. |
| 1:23 | Close + lunch launch (7′) | Achievement line; lunch slide; post return time + the spec-menu link ("worth five minutes with a sandwich"). | Lunch. | Lunch is a hard firewall – an overrun here may not cross it. |

### Block 3 – Preregister like an analyst (90′)

| Rel. | Segment | What I do | What participants do | Watch out for |
|---|---|---|---|---|
| 0:00 | Re-entry + roll-call (5′) | "Raise hand" roll-call; paste the catch-up card for late returners (text below). | Rejoin; late returners follow the card. | Do not wait past 0:05 – the card carries stragglers. |
| 0:05 | Hands-on 3: Task 4, the DAG (17′) | Half-built DAG slide; they wire the three candidate nodes and run `adjustmentSets()`; pairs in breakouts if available, chat variant otherwise; one share-out. | Finish the DAG; run `adjustmentSets()`; two-sentence FE note. | **Trigger at 0:20:** if the room is behind, switch to demo-only (proven in v1) – run it on my screen, they note the output. |
| 0:22 | Spec menu (10′) | Walk the one menu, every axis first-class – outcome, family, predictor, predictor form, copredictor, estimator, sample, weights; give the historical provenance (Multi100's Task 2 fixed unemployment and, implicitly, the gaussian scale; the menu opens both axes, and Task 1 shows three of five analysts freely chose GDP measures; beta fits report z, not t); state the **claim-alignment convention** (negative framings reverse-coded, 1 − y, applied automatically by `claim_align <- TRUE` – support = negative sign for everyone, and the class chart uses that scale); two worked examples of spec + one-sentence justification. The whole day works from the **corrected panel** (`EUframes_cy.csv`) – there is no separate as-submitted working file. | Pick their ONE deviation; draft the justification. Nobody runs anything. | Blank pairs: "what does your DAG say about growth – confounder or copredictor?" |
| 0:32 | OSF + preregistration (18′) | The three OSF addresses; **scavenger beat** (3′): open h7432, find the replication, paste the b in chat; Multi100's registered protocol; the AsPredicted eight questions and the mapping table, with time to dwell on the data-in-hand question. | Scavenge; read; ask. | The scavenger is a live-web moment – if OSF is slow, show it on my screen and keep the beat. |
| 0:50 | THE PREREGISTRATION MOMENT (10′) | Called from the front, synchronous. Complete fields 1–5 → render (Route 1) / save into script (Route 2) → type "declared". Count aloud ("eight declared… twelve…"). **Proceed rule:** move on at ~80% declared or at 10′, whichever first; stragglers finish while others start running. | Complete, render, declare. | Never cut. The rendered block is the record; the mirror form (if any) is silent and optional. |
| 1:00 | Run + submit (20′) | "Build and run your declared spec – only that one" (Task 6). Then `report_result()` (Task 7) – it takes no arguments: it reads the preregistered choices, auto-builds the spec label, always submits claim-aligned values, and prints the submission line plus a one-click pre-filled form link. Keep the Multiverse page shared; press *Run Code* at ~1:05, 1:12, 1:20 as dots land. Buddy-chunk rescues in the main room; check help doc at 1:08 and 1:16. Fast finishers → a second, exploratory spec (the first declaration was the preregistration; anything after it is exploration – naming which is which out loud is the point), or the extended-universe option, if GO. | Run, submit, watch the chart grow; a second exploratory spec if fast. | Everyone lands a dot: if the form will not open, they read the printed numbers aloud and I submit for them; the last-resort ladder ends at the grid lookup + hand submission. Watch the Sheet tab for malformed rows. |
| 1:20 | First look (10′) | Two questions from the bank (sign flip? significance loss?); find-your-dot. Post break time; countdown slide. | Locate their dot; discuss. | Leave the Multiverse page open over the break. |

### Block 4 – Reading the multiverse (90′)

| Rel. | Segment | What I do | What participants do | Watch out for |
|---|---|---|---|---|
| 0:00 | Class chart debrief (12′) | The class multiverse + the five-analyst overlay; question bank. | Discuss; find their dot against the analysts. | If submissions are few, the 840 grid + analysts still carries it. |
| 0:12 | Prediction → fork importance → analysts → flips (18′) | "Place your bet" waterfall FIRST; then the 49.7% reveal; the placement chart (person-level analysts at the extremes); the patterned flips. | Bet; watch the reveals; connect to their own axis choice. | The bet must come before the reveal – do not skip it to save time; it is what makes the 49.7% land. |
| 0:30 | Hands-on 4: your percentile (10′) | Point at the **browser lab's** percentile chunk – both routes use the lab cell (the workspace report has no percentile section); waterfall the results. | Compute their percentile; waterfall it. | Participants without a submission use the baseline (−3.804) – percentile ≈ 49.6% on the claim-aligned scale. |
| 0:40 | Stretch (2′) | Cameras off. | Stretch. | – |
| 0:42 | Inference literacy (18′) | Two jobs; not-840-tests; the permutation design; how to read B = 500; then the **discussion beat** ("70.7% – strong evidence? against what null?"): 2′ think, chat, harvest; then what-stands-without-the-test and the critics slide (the live 2025–26 debate – name the three positions, leave it open). | Think; argue in chat. | The target answer: "we can't say until we know what chance produces on these dependent data." Someone will get there – let them say it. |
| 1:00 | Taking it home (15′) | The git sketch; companion modules; take-home resources. | Note links. | Keep the git sketch to ~8′ – it is a signpost, not a lesson. |
| 1:15 | Wrap + feedback (15′) | Achievements restated; feedback form in chat; chart left on screen; thanks. | Feedback; goodbyes. | End on time – online goodbyes drag. |

---

## Online / in-person cue sheet

Same materials; only the interaction layer swaps.

| Beat | Online (Teams) | In person |
|---|---|---|
| Quiz / waterfall answers | type in chat, hold until "go" | write on paper, hands up on "go" (or Vevox if the venue has it) |
| Working / stuck status | green-circle emoji / "STUCK" in chat | green / red-orange sticky notes |
| Roll-call | Teams "raise hand" | visual sweep |
| Pair moments | breakout rooms if available; scripted chat variant otherwise | table pairs |
| Rescues | buddy-chunk on the shared screen | facilitator circulates |
| The live chart | everyone opens the Multiverse page themselves | projector + own laptops |
| Form links | pasted in chat | QR codes on the slides |
| Breaks | countdown slide + "back at :xx" in chat | countdown slide + verbal |
| Facilitator absent | published day plan = standing order | n/a |

---

## Chat protocols and ready-to-paste texts

**Waterfall:** "Type your answer now but DON'T send – hold it. … Go." Guarantees independent answers; produces the wall-of-identical-values beats.

**Help:** STUCK in chat flags; the standing **help doc** (shared editable doc, link seeded at open) is the second surface for pasted errors – check it at the named minutes in the run sheet, not continuously. Rescue in the main room via buddy-chunk (their spec, my webR/Positron, everyone learns).

**Silent room:** after 20 seconds, take reactions (thumbs) as the minimal response; name one or two people gently ("Sam, what did you get?"); two substantive answers from twenty participants is a proceed, not a failure.

**Catch-up card (paste at Block 3 re-entry):**

> Just back? You missed nothing you can't recover: (1) open your report's Task 4 (Estimand and DAG) section and run the DAG chunk as-is; (2) open the spec menu and pick ONE deviation; (3) when the room preregisters, complete the block and type "declared" like everyone else. Flag STUCK and I'll come to you in the run window.

**Facilitator-drop standing order (seeded in chat at open):**

> If I drop off the call: keep going. The day plan (link) has the timings – finish the current task or take the break early. I will rejoin as soon as I can.

---

## Compression ladders (with trigger times)

- **Block 1** – trigger 0:28: cut the Miske moderators slide to one line. Trigger 1:05 (hands-on dragging): the Task 3 fit becomes follow-my-screen for the stragglers; the waterfall still happens.
- **Block 2** – trigger ~1:00 (running long after estimands): the DAG crash course drops the spurious-by-design slide (one sentence instead) and reaches dagitty sooner. The story of the second number and the estimand waterfall are never compressed.
- **Block 3** – trigger 0:20: Task 4 (the DAG) to demo-only (the v1-proven variant). The prereg moment and the run window are never compressed.
- **Block 4** – trigger 0:55: inference module leans on the reading (two slides instead of four); the git sketch to a pointer. The class-chart debrief and the fork-importance reveal are never compressed.

**Never cut:** the five-analyst reveal · the story of the second number (the −3.853/−3.804 arc, the corrected-build annotation, the open OSF trail) · the preregistration moment · results submission + the live chart debrief · the fork-importance reveal.

---

## Contingency table

| Situation | What to do |
|---|---|
| **A participant's webR fails mid-task** | The cheatsheet ladder: hard reload + rerun chunks above → second browser → buddy-chunk on my screen → grid lookup + hand submission (any spec looks up in `data/spec_grid_full.csv`, the full 2,520-row universe). Everyone lands a dot. |
| **Route 1 machine breaks** | Switch to the browser lab (full parity); their workspace survives for later. |
| **The live chart fails during the day** (Form, Worker, CORS) | Rung 1: `results.qmd` falls back to the committed `data/class_results.csv` automatically. Rung 2: pull the Sheet CSV, commit, re-render locally, share my screen. Rung 3: the static 840-spec curve + five-analyst slides still carry the debrief. Submissions are safe in the Sheet regardless – nothing upstream blocks on the chart. |
| **Breakout rooms unavailable** | Every pair moment has a scripted chat variant. Zero structural cost. |
| **OSF slow/unreachable during the scavenger** | Show h7432 on my screen; the beat survives. Data never depends on OSF live – it ships with both routes. |
| **My connection drops** | The standing order in chat: participants continue the current task or break early; I rejoin. The pipeline keeps collecting – it is entirely independent of the call. |
| **A participant cannot speak/hear** | Everything essential is chat-and-screen; deliver any spoken instruction in chat on request. |
| **Recording objections** | Recording is optional by design – stop it; nothing depends on it. |
| **Prereg mirror form fails** | The rendered block is the record; carry on. |

---

## Answer keys and expected numbers

- Warm-up matrix question set: 1 = reproducibility (a failed attempt is still reproducibility) · 2 = the grey area between reproducibility and robustness – argue both ways · 3 = replicability · 4 = generalisability.
- Task 3 (both routes): b = −0.00347, SE = 0.00091, **t = −3.804**, df = 233, N = 270 (`EUframes_cy.csv`) – the value on record for C6HJR, reproduced directly from `euframes`.
- The story's first number (told in Block 2, not a participant task): b = −0.00341, SE = 0.00088, **t = −3.853**, df = 233, N = 270 (the as-submitted `moreh_rep/` pipeline; its exported artifact is `rep_data.csv`, which now lives only inside the story and on OSF).
- The divergence (the "other number in the record" slide; participants can verify it via the optional `forensics.R` exhibit): only 2009 diverges (mean |diff| 0.030, max 0.132; all other years 0.000); n_cy 2008 = 53,279 vs 2009 = 26,618, ratio exactly 0.500 (the tell is the 2008→2009 drop, not 2009's size – 2006/2007/2013 are also ≈26,600).
- The cause (the "one file, read twice" slide): prep chunks #8 (`eb701`) and #9 (`eb724`) both read `ZA4819_v3-0-2.dta`; ZA4994 (EB 72.4) is never read.
- Public-record beat (show on request): the Multi100 cleaning sheet records C6HJR's self-reported **t = −3.853** with the cleaned value **−3.804** and no cleaning note (`multi100_reanalyzed-effect-sizes_raw_data.csv`; local copies + provenance in `_planning/multi100/`) – the day's contrast sits in the public record.
- Depersonalising beat (the "same slip, one desk over" slide): KQXUE's own `task1.do`/`task2.do` double-append EB 62.0 (autumn 2004) – the same class of error, one desk over, present in both their submissions and caught by nobody. Two of the five pipelines in this one case carry a wave-set slip.
- Family axis (a first-class menu axis; not part of Multi100's Task-2 menu, which fixed unemployment and, implicitly, the gaussian scale): beta submissions report **z**, not t. In the browser lab every spec, beta included, is read from the pre-computed grid, so `report_result()` takes no fit; Route 1 fits beta live. Grid-lookup fallback: any spec looks up in `data/spec_grid_full.csv` (the full universe of 2,520 rows), with `data/spec_grid_family.csv` (the family universe of 1,680 rows = the 840 Gaussian rows + their beta twins) as its family-only sibling. Beta twin of the anchor: b(logit) = −0.01399, **z = −4.075**, r = −0.258 (Gaussian anchor r = −0.242: same direction, still significant, slightly stronger).
- Claim-alignment (the room's convention, set on the spec menu): negative framings (`mcomm`, `mlib`, `mneg`) reverse-coded 1 − y before fitting; sign of b, t and r flips, magnitudes and p untouched (exactly so for beta too). Aligned r runs **−0.70 to +0.24** (gaussian; beta reaches +0.29) – so a class dot above ≈ +0.25 is almost certainly an **un-recoded negative framing**, not a genuine contrary result; name that in the debrief. One caveat from the growth-predictor axis: legitimately aligned growth-predictor dots can reach ≈ +0.37 – check the declared spec before calling it.
- Analyst placement (share of grid more negative, claim-aligned): KEVF1 15.8% · KQXUE 21.5% · C6HJR 49.6% · 018OL 63.1% · PRL47 67.9%. The two person-level analysts (KEVF1, PRL47) sit at the extremes; the constrained baseline lands almost exactly at the aligned grid's median.
- Task-1 layer (slide after analyst placement): free phase = five different questions – 018OL GDP p.c. level, KEVF1 growth, PRL47 a GDP battery; only KQXUE and C6HJR used unemployment – with one shared recorded verdict ('evidence for the claim'). No standardised Task-1 effect sizes exist in the public Multi100 data (verdict + free text only). My own Task 1 (`mposframe ~ growth + unemp`, two-way FE, World Bank series) reproduces from the committed as-submitted panel: unemployment t = −4.72, growth ns – standardisation *weakened* my result.
- Predictor axis (a first-class menu axis; menu + lab lookup, `spec_grid_full.csv`, full universe 2,520 = the family universe of 1,680 + growth 840, both families; not part of Multi100's Task-2 menu, though Task 1 shows three of five analysts freely chose GDP measures): GDP growth as claim carrier, raw form only – affine forms are t-inert (centred≡raw, log-z≡log), which is why the deduplicated canon holds 840 distinct analyses. Growth predictor (840 rows in the full grid): 70.5% claim-direction / 46.9% significant, aligned max +0.37 – direction without significance, matching KQXUE's Task-1 growth null. Growth mirror of my Task-1 model on the corrected panel: b = +0.00167, t = 1.59, ns. Growth polarity: claim-aligned reporting flips the sign (lab lookup does it).
- Fork importance: outcome 49.7% · residual/interactions 49.2% · sample 0.7 · estimator 0.4 · predictor form/copredictor/weights ≈ 0.
- Sign flips (mcosmo universe, 140 specs): 41 wrong-sign; 33 in `pooled_cl`, 8 in `fe_twoway`; 24 of the 41 in bailout-excluding samples (halved from the pre-dedupe counts – every distinct analysis appeared exactly twice before, so shares are invariant).
- Constrained universe: 140 specs · 70.7% negative · 57.1% negative-and-significant. Discussion-beat target: "no verdict without a null for dependent specifications – that is what a curve-level permutation test would supply (the design in the inference reading)."
- Baseline percentile (for participants without their own submission): −3.804 ⇒ ≈ 49.6% on the claim-aligned scale.

---

## Debrief question bank

Use 4–6, reading the room.

1. **Sign flip:** "Did anyone's specification flip the sign? If not – after seeing the flips chart, where would you have had to stand in the menu to flip it?"
2. **Significance loss:** "Did any spec lose significance? Reason to doubt the claim, or to scrutinise that spec?"
3. **Axis that moved most:** "Before I showed the decomposition – which axis did you bet on, and why did most of us get it wrong?"
4. **Justification quality:** "Whose DAG settled their choice? Whose DAG could have justified three different specs equally well?"
5. **Preregistration felt:** "Did completing the block before running change how the result felt when it landed? Would you have chosen differently after seeing it?"
6. **Robustness tolerance:** "Multi100 used ±0.05 and ±0.20 d. Is the claim robust by the tight standard here? The loose one? Who gets to choose the tolerance for a policy claim?"
7. **Against the five analysts:** "The class overlaps the five analysts' range. Is that reassuring or circular? What would a result *outside* their range have meant?"
8. **Percentile literacy:** "Your percentile places you among 840 defensible alternatives. What does a 10th-percentile analyst owe their reader that a 50th-percentile analyst does not?"
9. **The three Rs on one paper:** "This paper failed reproduction, survived robustness in sign but not magnitude, and sign-flipped in replication. Which R should worry us most – and does it depend on what the finding is used for?"
10. **The inference gap:** "If a curve-level permutation test came back at p = .30, what would survive of today? What if it came back at p = .004?"

---

## After the session

- [ ] Run the reset procedure (Delivery lifecycle above): export + commit the date-stamped snapshot, rotate the Sheet rows.
- [ ] Export the feedback form responses.
- [ ] Improvement log – record: which segment ran longest; did the story of the second number land cleanly (attention held, the moral clear); how many declarations before the proceed rule fired; did every participant land a dot (which ladder rungs fired); did webR hold on home connections; Teams frictions (chat scale, breakouts, share switching).
